apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfood-webapi.fullname" . }}-test-connection"
  labels:
    {{- include "myfood-webapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfood-webapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
