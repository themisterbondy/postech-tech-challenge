apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfood-webapi.fullname" . }}
  labels:
    {{- include "myfood-webapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfood-webapi.selectorLabels" . | nindent 4 }}
